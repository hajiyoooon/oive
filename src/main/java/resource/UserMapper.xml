<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.UserMapper">
   	<select id="selectUser" parameterType="hashmap" resultType="vo.UserVO">
    	select * from users
    	where user_id = #{userId} and password = #{password}
 	</select>
 	
 	<insert id="insertUser" parameterType="vo.UserVO">
 		<selectKey resultType="int" keyProperty="id" order="BEFORE">\
 			select users_seq.nextval from dual
 		</selectKey>
    	insert into users(user_id, password, birthday, email)
    	values(#{userId},#{password},#{birthday}, #{email})
 	</insert>

</mapper>









<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.UploadMapper">
	<select id="listAll" resultType="vo.FileVO">
		select fileid, id, uploaddate, filename, profilecategory from files
		where userid = #{userId}
	</select>
	
	<insert id="upload" parameterType="int">
		insert into files(FILEID,UPLOADDATE,USERID,FILENAME)
		values (file_seq.nextval, sysdate, 'test', 'testfilename')
	</insert>
	<update id="update">
		update files
		set filename = #{fileName}
		where userid= #{userId} and fileid = #{fileId}
	</update>
	<delete id="delete">
		delete from files
		where userid= #{userId} and fileid = #{fileId}
	</delete>
</mapper>








